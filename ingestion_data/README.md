# Data preparation

First, a CAS json generated using the ingest_data function. Then a [custom script](https://github.com/hkir-dev/experimental_notebooks/blob/main/BG_taxonomy/BG_consensus_taxonomy.ipynb) is used to resolve literature strings to DOIs and add CL terms labels.

see `input_data` folder for the latest json.